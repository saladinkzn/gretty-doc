buildscript {
  repositories {
    jcenter()
  }

  dependencies {
    classpath 'org.ajoberstar:gradle-git:1.4.2'
  }
}

apply plugin: 'org.ajoberstar.github-pages'

githubPages {
  repoUri = 'git@github.com:saladinkzn/gretty-doc.git'
  pages {
    from(file('build/jbake')) {
   	  into '.'                                    
    }
  }
}
